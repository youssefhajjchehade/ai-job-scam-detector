# Data

The raw dataset is intentionally not committed to GitHub because it is large.

Training used the Kaggle dataset commonly known as **Real or Fake Job Posting Prediction** / `fake_job_postings.csv`.

To retrain the notebook pipeline, download the CSV and place it here:

```text
data/fake_job_postings.csv
```
